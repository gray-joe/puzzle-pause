[pytest]
addopts = --cov=app --cov-report=term-missing --cov-report=html:htmlcov
testpaths = tests
markers =
    api: HTTP API integration tests (FastAPI TestClient)
    unit: Pure unit tests (no HTTP layer)
